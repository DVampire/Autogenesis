---
name: command_default
description: "Implements lifecycle commands for create, inspect, evaluate, evolve, copy, checkpoint, restore, rollback, version listing, deprecation, and unregistration. Shared parsing helpers remain private in `_helpers.py`."
version: 1.0.0
type: collection
category: command
requirements: []
metadata: {}
---
# Built-in commands

Implements lifecycle commands for create, inspect, evaluate, evolve, copy, checkpoint,
restore, rollback, version listing, deprecation, and unregistration. Shared parsing helpers
remain private in `_helpers.py`.
